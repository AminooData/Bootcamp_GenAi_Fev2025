Liste = [("name","Elie"),("job","instructor") ]
dictionnary = dict(Liste)
print(dict)

keys = ["CA", "NJ", "RI"]
values = ["California", "New Jersey", "Rhode Island"]

state_dict = dict(zip(keys, values))  # Associer avec zip() et convertir en dictionnaire
print(state_dict)

# 📌 On crée une liste contenant les voyelles
vowels = ["a", "e", "i", "o", "u"]

# 📌 On crée un dictionnaire vide
vowel_dict = {}

# 📌 On remplit le dictionnaire avec les voyelles comme clés et 0 comme valeur
for letter in vowels:  # 🔄 On parcourt chaque voyelle
    vowel_dict[letter] = 0  # 📌 On ajoute {voyelle: 0} au dictionnaire

# 📌 On affiche le dictionnaire
print(vowel_dict)  # {'a': 0, 'e': 0, 'i': 0, 'o': 0, 'u': 0}

import string  # 📌 On importe la bibliothèque qui contient l'alphabet

# 📌 On récupère toutes les lettres en minuscules
letters = string.ascii_lowercase  # "abcdefghijklmnopqrstuvwxyz"

# 📌 On crée un dictionnaire vide
alphabet_dict = {}

# 📌 On associe chaque lettre à sa position dans l'alphabet
for index, letter in enumerate(letters, start=1):  # 🔄 On compte à partir de 1
    alphabet_dict[index] = letter  # 📌 {1: 'a', 2: 'b', ...}

# 📌 On affiche le dictionnaire
print(alphabet_dict)
